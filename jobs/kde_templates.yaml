- job-group:
    name: 'frameworks-{item}'
    arch:
        - amd64
        - i386
        - armhf
        - armel
    jobs:
        - '{item}_prepare'
        - '{item}_build'
        - '{item}_test':
            dependencies: '{obj:test_dependencies}'
            arch: amd64
    dependencies: '{obj:build_dependencies}'
    description: '{description}'
    debian_vcs: '{debian_vcs}'
    upstream_vcs: '{upstream_vcs}'

- job-group:
    name: 'plasma-{item}'
    arch:
        - amd64
        - i386
        - armhf
        - armel
    jobs:
        - '{item}_prepare'
        - '{item}_build'
        - '{item}_test':
            dependencies: '{obj:test_dependencies}'
            arch: amd64
    dependencies: '{obj:build_dependencies}'
    description: '{description}'
    local_vcs: '{local_vcs}'
    debian_vcs: '{debian_vcs}'
    upstream_vcs: '{upstream_vcs}'

- job-group:
    name: 'applications-{item}'
    arch:
        - amd64
        - i386
        - armhf
        - armel
    jobs:
        - '{item}_prepare'
        - '{item}_build'
        - '{item}_test':
            dependencies: '{obj:test_dependencies}'
            arch: amd64
    dependencies: '{obj:build_dependencies}'
    description: '{description}'
    debian_vcs: '{debian_vcs}'
    upstream_vcs: '{upstream_vcs}'

- defaults:
    name: global
    quiet-period: 5
    block-downstream: false
    block-upstream: true
    retry-count: 3
    logrotate:
        daysToKeep: 7

- job-template:
    name: '{item}_prepare'
    description: '{description} -- Prepare'
    scm:
        - git:
            basedir: 'repo'
            remotes:
                - local:
                    url: '{local_vcs}'
                - debian:
                    url: '{debian_vcs}'
                - upstream:
                    url: '{upstream_vcs}'
            branches:
                - master
                - debian/master
                - local/master
            local-branch: master
            wipe-workspace: false
            skip-tag: true
            prune: true
    triggers:
        - pollscm:
            cron: 'H * * * *'
        - script:
            script:
                !include-raw-escape: prepare_trigger.sh
            cron: 'H H/6 * * *'
    builders:
        - shell:
            !include-raw-escape: prepare_build.sh
    publishers:
        - trigger:
            project: '{item}_build'
            threshold: UNSTABLE
        # Needs the clone workspace scm plugin
        - clone-workspace:
            criteria: "not failed"
            workspace-glob: "**"

- job-template:
    name: '{item}_build'
    project-type: matrix
    axes:
        - axis:
            type: slave
            name: arch
            values:
                - amd64
                - armel
                - armhf
                - i386
    description: '{description} -- Build'
    triggers:
        - reverse:
            jobs: '{obj:dependencies}'
            result: 'unstable'
    publishers:
        - trigger:
            project: '{item}_test'
            threshold: UNSTABLE
        - archive:
            artifacts: 'build/**'
    # Needs the clone workspace scm plugin
    scm:
        - workspace:
            parent-job: '{item}_prepare'
            criteria: 'Not Failed'
    builders:
        - shell:
            !include-raw-escape: build_build.sh

- job-template:
    name: '{item}_test'
    description: '{description} -- Test'
    block-upstream: false
    node: amd64
    triggers:
        - reverse:
            jobs: '{obj:dependencies}'
            result: 'unstable'
    # Needs the clone workspace scm plugin
    builders:
        - shell:
            !include-raw-escape: test_build.sh
    publishers:
        - archive:
            artifacts: 'build/**log'
        - junit:
            results: 'build/*.xml'
